# Changelog

## 0.1.0 (unreleased)

- Initial scaffold: `justpkg-core` (HTTP trait, path sanitizer, error types),
  `justpkg-nix` (flake.lock parser, narinfo fetcher, NAR extractor),
  `justpkg-cli` (`justpkg build/fetch/extract` commands)
