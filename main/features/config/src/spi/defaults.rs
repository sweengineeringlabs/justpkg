/// Default Nix binary cache URL. Overridable via `[nix] cache_base` in `application.toml`.
pub const CACHE_BASE_DEFAULT: &str = "https://cache.nixos.org";

/// Default NixOS channel index base URL. Overridable via `[nix] channel_base` in `application.toml`.
pub const CHANNEL_BASE_DEFAULT: &str = "https://channels.nixos.org";
