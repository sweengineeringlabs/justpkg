[package]
name        = "swe_justpkg_config"
version     = "0.1.0"
edition     = "2021"
description = "Application configuration loader (XDG) for justpkg tools"

[dependencies]
serde = { workspace = true }
toml  = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
